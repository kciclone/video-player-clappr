describe('Testing Video Player Component', function () {

    it('Should be play video', function () {
        browser.get('demo/index.html');

        var component = element(by.id('playerComponent'));
        
    });
});
